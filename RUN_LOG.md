# Overnight Run Log: bagz license, rebrand, dep refresh

Started: 2026-05-14T22:50:43Z
Branch: overnight/bagz-rebrand-refresh-20260515
Plan file: /Users/bioharz/git/zcash/bagz/plans/polished-cooking-flute.md

## Tiers

- 0-setup: created branch, pushed draft PR #2.
- 0a-baseline-clippy: baseline strict clippy failed before plan edits; fixed existing clippy-only issues in `bagz-engine` and restored the light Rust gate. Last green: `ad95c13e3734ea837b045704577e53874b248180`.
- 1-license-polyform-shield: replaced MIT with official PolyForm Shield License 1.0.0 text from `https://polyformproject.org/licenses/shield/1.0.0.txt`, using `Copyright (c) 2026 devdotbo (Reza Shokri)`. Previous LICENSE header was `Copyright (c) 2025 devdotbo (dev.bo)`. `cargo metadata --no-deps --format-version 1` and `cargo check --workspace --exclude bagz-app-tauri` passed. Last green: `cc1f1306df3dd3ca2660d0fd23b95e7c6530cf70`.
- 2-ring-provider-fix: cherry-picked `7469fd4` as `ae0c7cecf4ec3a00307cbfc501c789328217eda5`. `make fmt-check`, strict clippy excluding the Tauri app, and `cargo test -p bagz-network` passed.
- 3-ring-provider-regression-test: added `ring_provider_installs_and_is_default`. `cargo fmt --all --check` and `cargo test -p bagz-network ring_provider_installs_and_is_default` passed. Last green: `5d27f2cb1d82b09bf1f26642dcadaa0dd5ed6aa4`.
- 4a-tauri-cef-rev-bump: bumped `tauri` and `tauri-build` patch rev to upstream `tauri-apps/tauri@6fd733b2d6255d358e88ad19cb15dc7d22b293ac` (`feat/cef`, 2026-05-14). This moves Rust Tauri to `2.11.1`, `tauri-build` to `2.6.1`, and CEF from `146.4.1+146.0.9` to Tauri-pinned `148.0.0+147.0.10` (latest standalone `cef-rs` is `148.1.0+147.0.14`, but Tauri runtime pins `148.0.0`). Synced `@tauri-apps/api` to `2.11.0` and `@tauri-apps/cli` to `2.11.1` because the new Tauri CLI rejects mismatched JS/Rust minor versions. Fixed the test bridge build by enabling `tauri/wry` only for `test-bridge` and by passing `SensitiveString` by value in the restore bridge handler. `make check`, `make tauri-build`, CEF framework presence check, and `make test-e2e` passed. Bundle outputs: `target/release/bundle/macos/bagZ.app`, `target/release/bundle/dmg/bagZ_0.2.1_aarch64.dmg`. Last green: `ec3b3681b183cb24482babce74c09aa57fe0a245`.
- 4b-js-minor-bumps: refreshed frontend packages while keeping Vite on `7.3.3` and `@vitejs/plugin-react` on `4.7.0`; Vite 8 remains isolated for its own tier. Chose `@zxing/library 0.22.0` with `@zxing/browser 0.2.0` because the browser package declares peer `@zxing/library ^0.22.0`; standalone `0.23.0` is newer but not peer-compatible. `bun run build` and `make test-e2e` passed after installing the Playwright 1.60 Chromium binary. Last green: `819c5d0ed57b74caf9e5ab0d2c347ad2e8f05b92`.
- 5-rust-transitive-patch-updates: `cargo update --workspace` was a no-op after the Tauri/JS tiers (`0 packages`). Verified anyway: `make check` and `make tauri-build` passed; bundle still outputs `target/release/bundle/macos/bagZ.app` and `target/release/bundle/dmg/bagZ_0.2.1_aarch64.dmg`. Last green remains `819c5d0ed57b74caf9e5ab0d2c347ad2e8f05b92`.
- 6-zcash-crate-bumps: bumped `zcash_client_backend` to `0.22.0`, `zcash_client_sqlite` to `0.20.2`, `zcash_primitives` to `0.27.1`, `zcash_proofs` to `0.27.0`, `zcash_protocol` to `0.8.0`, `zcash_transparent` to `0.7.0`, `pczt` to `0.6.0`, and `orchard` to `0.13.1`; this also pulled `shardtree 0.6.2`. Applied the required API updates for `BlockRange.pool_types`, `TransparentOutputFilter::All`, and the new optional transaction version arguments. Fixed a real shielding fee mismatch exposed by the upgrade: the custom P2PKH input fee view used a stale 149-byte literal, while ZIP 317 and the upgraded builder use `InputSize::STANDARD_P2PKH` (150 bytes). `make fmt-check`, `make check`, `make tauri-build`, CEF framework presence check, `cargo tree -i aws-lc-rs` (no matching package), and `make test-e2e` passed. Bundle outputs: `target/release/bundle/macos/bagZ.app`, `target/release/bundle/dmg/bagZ_0.2.1_aarch64.dmg` (191 MB). Last green: `ca10ea921c3b7406d2a61cd50a841945f51343db`.
- 7-aggressive-transitive-update: ran `cargo update --aggressive`, producing a lockfile-only update across 220 packages. Notable versions after the update: `rustls 0.23.40`, `rustls-platform-verifier 0.7.0`, `reqwest 0.13.3`, `tonic 0.14.6`, `tokio 1.52.3`, `hyper 1.9.0`, `tauri-plugin 2.6.1`, and `tauri-utils 2.9.1`; `aws-lc-rs` remains absent from the dependency graph. `cargo test -p bagz-network ring_provider_installs_and_is_default` and `make check` passed. Last green: `97ad81de7544ec218fe0986e35228a4d5019036f`.
- 8-vite-8: bumped `vite` to `8.0.13` and `@vitejs/plugin-react` to `6.0.2`. `bun run build` passed without config migration edits, and `make test-e2e` passed 17/17. The bridge run still logs known dev-console menu-listener errors because the browser test page lacks the real Tauri callback transform API; this did not break tested flows. `bun outdated` now reports only `@zxing/library 0.22.0 -> 0.23.0`, intentionally held because `@zxing/browser 0.2.0` peers on `@zxing/library ^0.22.0`. Last green: `0180cc38c51ac23efd56b1e000cb9357d411ae47`.
- 9-cargo-audit: `cargo audit` exited 0 with no vulnerabilities. Compared JSON output against `origin/dev`: no new advisory IDs on the branch. Warning counts improved from dev's 21 unmaintained / 5 unsound / 2 yanked warnings to this branch's 20 unmaintained / 2 unsound warnings. No security snapshot doc was committed because there was no new advisory delta.
- 10-rebrand-directories: renamed workspace member directories from the legacy brand prefix to `bagz-*`, including the Tauri app, CLI, crate directories, and desktop wallet spec directory. This commit intentionally broke manifests until the package-name tier restored the paths.
- 11-rebrand-cargo-packages: renamed Cargo package names, dependency keys, app lib crate name, and CLI binary to `bagz-*` / `bagz`. `cargo metadata --no-deps --format-version 1` passed and listed only `bagz-*` workspace packages.
- 12-rebrand-code-docs-ipc-env: bulk-renamed source, docs, IPC command strings, environment variables, scripts, CI, and specs from the legacy brand to bagz while excluding generated output, binary assets, `Cargo.lock`, and `LICENSE`. `cargo check --workspace --exclude bagz-app-tauri` and `cd apps/bagz-app-tauri && bun run tsc --noEmit` passed. No old brand spelling remains in tracked non-generated text or filenames after the later asset tier.
- 13-ipc-env-runtime-audit: audited Bagz IPC/env surfaces (`bagz_*` commands, `BAGZ_*` env vars, server-resolver child marker) and fixed the Tauri app-handle command paths to be generic over `R: Runtime`. This keeps production on CEF while allowing `cargo test -p bagz-app-tauri --no-default-features` and `cargo build -p bagz-app-tauri --features test-bridge` to compile cleanly.
- 14-rebrand-assets-and-bundle-metadata: renamed SVG asset files and platform build docs to Bagz names. Verified Tauri metadata already used `productName = "bagZ"`, `identifier = "app.bagz.desktop"`, and window title `bagZ`. `cd apps/bagz-app-tauri && bun run build` passed. Note: `bagZ_Wordmark.svg` is still the old vector wordmark artwork internally and is not currently imported by the UI; redraw/replace it before using that asset in-product.
- 15-full-rust-validation: `cargo update -w` was a no-op after the package rename (`0 packages`). `make fmt-check`, `make clippy-strict`, `make test`, `make test-migrations`, `cargo test -p bagz-app-tauri --no-default-features`, and `cargo build -p bagz-app-tauri --features test-bridge` passed. `cargo tree -i aws-lc-rs` still reports no matching package.
- 16-cef-build-and-bridge-e2e: `make tauri-build` passed with Tauri `feat/cef` and produced `target/release/bundle/macos/bagZ.app` plus `target/release/bundle/dmg/bagZ_0.2.1_aarch64.dmg`. CEF framework is present at `bagZ.app/Contents/Frameworks/Chromium Embedded Framework.framework`. Bundle sizes: app `382M`, DMG `192M`. Removed the stale generated pre-rebrand macOS app bundle after the new bundle was present. `make test-e2e` passed 17/17; the known browser-only menu listener `transformCallback` errors still appear in the Vite console because the Playwright page is not a real Tauri webview.
- 17-packaged-app-smoke: forced CI gRPC probe passed for `https://zec.rocks:443`. The same probe against `https://lwd.testnet.zec.pro` timed out from this machine tonight, so treat that endpoint as flaky/unavailable until rechecked. Launched `target/release/bundle/macos/bagZ.app/Contents/MacOS/bagz-app-tauri` for 15 seconds with isolated `HOME=/tmp/bagz-cef-smoke.*` and `BAGZ_GRPC_URL=https://zec.rocks:443`; process stayed alive and terminated cleanly, with no panic/fatal/abort patterns in the smoke log. Last green: `8be0203812738b8ead2511b9f1f31d5bf7a1eef7`.
- 18-endpoint-followup: local gRPC probes confirmed `https://zec.rocks` and `https://zec.rocks:443` pass, `https://testnet.zec.rocks` passes, `https://grpc.zec.rocks:443` fails DNS, and both `https://lwd.zec.pro` and `https://lwd.testnet.zec.pro` time out from this machine. Switched CI and seeded defaults to the working zec.rocks endpoints.
- 19-stabilization-followup: aligned Tauri/package metadata to workspace version `0.2.2`, added app DB migration v3 for existing seeded endpoint rows, and revalidated with `make fmt-check`, `cargo test -p bagz-engine --test app_db_migrations --test server_resolver`, `CI=true BAGZ_GRPC_URL=https://testnet.zec.rocks cargo test -p bagz-network --test ci_lightwalletd_probe -- --nocapture`, `CI=true BAGZ_GRPC_URL=https://testnet.zec.rocks make check`, and `make tauri-build`. Bundle outputs are now `target/release/bundle/macos/bagZ.app` and `target/release/bundle/dmg/bagZ_0.2.2_aarch64.dmg`; packaged-app 15s smoke passed with CEF present and no fatal log patterns.
