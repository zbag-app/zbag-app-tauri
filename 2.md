# Open Items Checklist (what’s left)

As of **2025-12-24**, these items are still outstanding. Each numbered item is intended to be **standalone** so you can hand it to a fresh context/agent without needing to re-read everything.

1. **Fix `research.md` conflict: backup verification says “never display seed again”, but “View seed phrase” is now permitted**
   - **Problem**: `specs/001-zkore-desktop-wallet/research.md:168` still says: `- UI: Never display full seed phrase after initial creation`, but the spec + constitution explicitly permit user-initiated re-display behind manual wallet-password re-auth.
   - **Files**:
     - `specs/001-zkore-desktop-wallet/research.md:168`
     - `specs/001-zkore-desktop-wallet/spec.md:273` (FR-008b)
     - `.specify/memory/constitution.md:41` (permitted mnemonic flows)
   - **Recommendation (edit)**: Replace the single bullet in `research.md` with something aligned, e.g.:
     ```md
     - UI: Display the full seed phrase only in permitted mnemonic flows (CreateWallet display, RestoreWallet entry, and user-initiated ViewSeedPhrase behind manual wallet-password re-auth). Otherwise never display, persist, or log it; clear UI state after the flow completes.
     ```
   - **Done when**: No doc asserts “never display after creation” without the explicit permitted-flow exception.

2. **Fix `data-model.md` conflict: “Mnemonic storage options (choose one)” vs mandatory wallet-password encryption**
   - **Problem**: `specs/001-zkore-desktop-wallet/data-model.md:53` frames mnemonic persistence as “choose one per deployment” and lists “OS Keychain (preferred)”, which reads like keychain-only protection; this conflicts with `specs/001-zkore-desktop-wallet/spec.md:352` (**NFR-009** requires wallet-password encryption at rest for spend-capable secrets).
   - **Files**:
     - `specs/001-zkore-desktop-wallet/data-model.md:40`
     - `specs/001-zkore-desktop-wallet/spec.md:352`
   - **Recommendation (edit)**: Rewrite the “Mnemonic storage options” block so wallet-password encryption is always the primary protection whenever anything is persisted; keychain is only a convenience layer (stores DEK / unlock material, not plaintext mnemonic).
     - Suggested replacement block:
       ```md
       **Mnemonic persistence (v1):**
       - If the mnemonic is persisted anywhere, it MUST be encrypted using the user-defined wallet password (per the v1 key hierarchy in plan.md: wallet password → Argon2id KEK → unwrap per-wallet DEK).
       - OS keychain "remember unlock" is OPTIONAL and stores only unlock material (DEK or a wrapping secret), never as the sole protection for the mnemonic and never as a substitute for per-action re-auth.
       
       **Mnemonic storage modes:**
       1. **Encrypted blob on disk (default):** Store an encrypted mnemonic blob in the wallet directory (or other backend-controlled storage). Decrypt requires wallet password (unless auto-unlock is enabled via keychain-stored DEK).
       2. **Memory-only mode (optional):** Do not persist mnemonic at rest; user must re-enter it on each app launch.
       ```
   - **Done when**: Reading `data-model.md` cannot be interpreted as “keychain-only is sufficient” for NFR-009 compliance.

3. **Document keychain auto-unlock semantics (it’s implied in plan/tasks, but not explicit in IPC comments)**
   - **Problem**: Plan/tasks allow “remember unlock” and mention auto-unlock on launch, but IPC doesn’t state whether `LoadWallet` attempts auto-unlock or how `lock_status` should be interpreted.
   - **Files**:
     - `specs/001-zkore-desktop-wallet/contracts/ipc-v1.ts:281` (`LoadWalletRequest`)
     - `specs/001-zkore-desktop-wallet/tasks.md:105` (auto-unlock mentioned)
     - `specs/001-zkore-desktop-wallet/plan.md:190` (prompt on launch unless keychain auto-unlock enabled)
   - **Recommendation (edit)**:
     - Add a sentence to the `LoadWalletRequest` comment clarifying that loading a wallet **may attempt OS keychain auto-unlock** if configured, and that `LoadWalletResponse.lock_status` reflects the post-attempt state.
     - Add/adjust a task so implementation doesn’t miss it (either new task or amend `T044d/T056a`) to explicitly implement “attempt keychain auto-unlock on wallet load/open”.
   - **Done when**: A reader can answer “when does auto-unlock happen?” by reading IPC comments + tasks without guessing.

4. **Standardize timestamp units across IPC + data model (seconds vs milliseconds is still ambiguous)**
   - **Problem**: Only some fields specify units (e.g. `BackupChallenge.expires_at` says “unix seconds”), most timestamps don’t. This risks subtle frontend bugs.
   - **Files**:
     - `specs/001-zkore-desktop-wallet/contracts/ipc-v1.ts` (multiple timestamp fields; e.g. `WalletInfo.created_at` at `:48`, `TransactionInfo.created_at` at `:106`, `RestoreWalletRequest.birthday_date` at `:435`)
     - `specs/001-zkore-desktop-wallet/data-model.md` (defines `Timestamp` but doesn’t specify units)
   - **Recommendation (decision + edit)**:
     - Pick a single standard (recommended: **unix seconds UTC** to match existing `BackupChallenge.expires_at`).
     - Add explicit `/** unix seconds */` (or equivalent) comments to all timestamp fields in IPC.
     - Add a short note in `data-model.md` defining what `Timestamp` means (e.g., “All Timestamp fields are unix seconds (UTC) unless otherwise stated.”).
   - **Done when**: Every timestamp field in IPC has an unambiguous unit; `data-model.md` defines `Timestamp` units.

5. **Make the broadcast retry queue compatible with the condensed constitution wording**
   - **Problem**: `specs/001-zkore-desktop-wallet/plan.md:268` explicitly allows persisting **signed tx bytes** (encrypted) for a broadcast retry queue. But `.specify/memory/constitution.md:103` says “Avoid storing raw payloads” with no explicit exception—this invites future “is this allowed?” churn.
   - **Files**:
     - `.specify/memory/constitution.md:96` (Data Minimization)
     - `specs/001-zkore-desktop-wallet/plan.md:268` (broadcast retry queue)
   - **Recommendation (edit)**: Amend `.specify/memory/constitution.md` Data Minimization to allow a narrow exception for resuming explicit user-initiated operations, under strict constraints. Example:
     ```md
     - Avoid storing raw payloads except when strictly required to resume an explicit user-initiated operation (e.g., queued broadcast retry). Any such payloads MUST be stored encrypted, retention-bounded, and MUST NOT be logged or sent to the UI.
     ```
   - **Done when**: A reviewer can reconcile “avoid raw payloads” with “encrypted queued signed bytes for retry” without ambiguity.

6. **Resolve status-marker mismatch (Draft vs Complete)**
   - **Problem**: `specs/001-zkore-desktop-wallet/spec.md:4` says **Status: Draft**, while `plan.md`, `research.md`, and `data-model.md` say **Complete**.
   - **Files**:
     - `specs/001-zkore-desktop-wallet/spec.md:4`
     - `specs/001-zkore-desktop-wallet/plan.md:4`, `specs/001-zkore-desktop-wallet/research.md:4`, `specs/001-zkore-desktop-wallet/data-model.md:4`
   - **Recommendation (decision + edit)**:
     - Decide on a single status vocabulary (e.g., `Draft` / `Ready for implementation` / `Complete`).
     - Update the headers so they are consistent and process-meaningful.
   - **Done when**: All feature docs for `001-zkore-desktop-wallet` agree on the same status meaning.

7. **Add a lightweight traceability matrix (still missing)**
   - **Problem**: Traceability is currently implicit; `tasks.md` notes traceability labels but there’s no concise mapping table.
   - **Files**:
     - `specs/001-zkore-desktop-wallet/tasks.md:634`
   - **Recommendation (add)**:
     - Add a short “Traceability” section to `tasks.md` **or** create `specs/001-zkore-desktop-wallet/traceability.md`.
     - Minimal format example:
       ```md
       | User Story | Covers FRs |
       |---|---|
       | US1 Create/Receive/Backup | FR-001..FR-004, FR-008a..FR-008b |
       | US2 Send | FR-009..FR-014 |
       | US3 Shield transparent | FR-010..FR-011 |
       ```
       And a second table mapping NFRs to task groups (logging, encryption, IPC validation, a11y).
   - **Done when**: Reviewers can quickly verify “this set of tasks covers these requirements”.

8. **Clarify memo-length semantics (bytes vs characters)**
   - **Problem**: `specs/001-zkore-desktop-wallet/data-model.md:144` says memo max **512 bytes**, but IPC + tasks don’t state how to measure length for a JS string (`MEMO_TOO_LONG` exists at `ipc-v1.ts:785`).
   - **Files**:
     - `specs/001-zkore-desktop-wallet/data-model.md:144`
     - `specs/001-zkore-desktop-wallet/contracts/ipc-v1.ts:363` (`PrepareSendRequest.memo`)
     - `specs/001-zkore-desktop-wallet/contracts/ipc-v1.ts:785` (`MEMO_TOO_LONG`)
   - **Recommendation (edit)**:
     - Add one sentence clarifying enforcement is by **byte length after encoding to the on-chain memo format**, not JS character count.
     - Optionally add a short comment on `PrepareSendRequest.memo` and/or in `data-model.md`.
   - **Done when**: Frontend/backend implementers won’t accidentally validate by JS `.length`.

9. **(Optional but recommended) Add a dedicated error code for swaps disabled on Testnet**
   - **Problem**: `specs/001-zkore-desktop-wallet/spec.md:399` says swaps must be disabled for Testnet wallets (NEAR Intents mainnet-only), but IPC error codes only have `SWAP_FAILED`.
   - **Files**:
     - `specs/001-zkore-desktop-wallet/spec.md:399`
     - `specs/001-zkore-desktop-wallet/contracts/ipc-v1.ts:803` (swap errors)
   - **Recommendation (add)**:
     - Add something like `SWAP_UNSUPPORTED_NETWORK` (or `SWAP_DISABLED_ON_TESTNET`) and use it consistently for swap commands when `wallet.network === Testnet`.
     - Document it in spec/tasks so UI can show a clean “Swaps are mainnet-only” message.
   - **Done when**: Swap-disabled behavior is explicit, stable, and user-explainable without overloading `SWAP_FAILED`.

10. **Sync canonical `docs/constitution.md` with the updated `.specify/memory/constitution.md` (Sapling support + transparent-recipient acknowledgement)**
   - **Problem**: `docs/constitution.md:123` still states “Orchard-only spending”, while `.specify/memory/constitution.md:58` already allows shielded spending via Orchard + Sapling, and the new spec allows transparent recipients with explicit acknowledgement.
   - **Files**:
     - `docs/constitution.md:123` (Article 4.1)
     - `.specify/memory/constitution.md:56` (Shielded-by-Default Privacy)
     - `specs/001-zkore-desktop-wallet/spec.md:276` / `spec.md:277` (Sapling support + transparent recipient ack)
   - **Recommendation (decision + edit)**:
     - Decide whether the constitution should require **Orchard-only** or allow **Orchard + Sapling** spending (the condensed constitution and feature spec currently say Orchard + Sapling).
     - Add an explicit rule for **transparent recipients**: allowed only with explicit privacy acknowledgement; memos not allowed for transparent recipients; transparent-input spends remain forbidden.
   - **Done when**: `docs/constitution.md` and `.specify/memory/constitution.md` no longer contradict each other on shielded pools and transparent-recipient policy.

11. **Sync `docs/plan.md` to match the current policy (it still says “Orchard-only shielded operations”)**
   - **Problem**: `docs/plan.md:35` says “Orchard-only shielded operations (no Sapling spending, no transparent spending)”, which is out of date relative to the feature spec + condensed constitution.
   - **Files**:
     - `docs/plan.md:35`
     - `specs/001-zkore-desktop-wallet/plan.md:46` (already describes Orchard+Sapling + transparent-recipient ack)
   - **Recommendation (edit)**: Update the statement to reflect the current stance (Sapling supported; transparent-input spends forbidden; transparent recipients allowed only with explicit ack).
   - **Done when**: `docs/plan.md` doesn’t reintroduce the old “no Sapling / reject transparent” assumptions.

12. **Sync `docs/spec.md` to match current policy (it contains multiple “Orchard only / no Sapling support” statements)**
   - **Problem**: `docs/spec.md` has multiple outdated assertions:
     - `docs/spec.md:37` and `docs/spec.md:224` (“Orchard only” headings)
     - `docs/spec.md:272` (“No Sapling pool support…”)
   - **Files**:
     - `docs/spec.md:37`
     - `docs/spec.md:224`
     - `docs/spec.md:272`
     - `specs/001-zkore-desktop-wallet/spec.md:58` (Sapling + transparent recipients allowed)
   - **Recommendation (edit)**: Update those sections to match the current send/recipient policy (UA→Orchard→Sapling selection; transparent recipients allowed with explicit ack; memo rules; transparent funds must be shielded before spending).
   - **Done when**: `docs/spec.md` no longer claims Orchard-only / no Sapling support.

13. **Sync `docs/task.md`: it still says “Reject transparent recipient sends”**
   - **Problem**: `docs/task.md:307` says “Reject transparent recipient sends”, but the current spec requires allowing transparent recipients with explicit privacy acknowledgement (and disallowing memos).
   - **Files**:
     - `docs/task.md:307`
     - `specs/001-zkore-desktop-wallet/spec.md:68` / `spec.md:277` (transparent recipient ack requirement)
     - `specs/001-zkore-desktop-wallet/tasks.md:200` (already specifies `PRIVACY_ACK_REQUIRED` + `MEMO_NOT_ALLOWED`)
   - **Recommendation (edit)**:
     - Replace “Reject transparent recipient sends” with:
       - “Require explicit privacy acknowledgement for transparent recipients”
       - “Reject memos for transparent recipients”
   - **Done when**: `docs/task.md` doesn’t contradict the current send policy.

14. **Final “no drift” verification pass (cheap and prevents rework)**
   - **Goal**: After addressing items above, confirm no old policy strings remain and that docs/specs are consistent on the big rules.
   - **Suggested checks**:
     - Search for old policy strings:
       - `rg -n \"Orchard-only|no Sapling|Reject transparent recipient|Never display full seed|choose one per deployment|unix timestamp\" docs specs .specify`
     - Spot-check that these align everywhere:
       - “View seed phrase” allowed only behind manual re-auth
       - “Shield transparent funds before spending” enforced
       - Transparent recipients allowed only with explicit acknowledgement; memos disallowed
       - Wallet DB encryption at rest (SQLCipher) and wallet-password key hierarchy
       - Timestamp units documented consistently
